# Security and artifact privacy

Casefiles are local files. Nothing is uploaded by this package. Traces and videos are excluded by default because masking their internal content cannot be guaranteed. Review casefiles before sharing them, configure screenshot masks for sensitive regions, and extend `redactHeaders` for application-specific secrets.

Report vulnerabilities privately through the security contact configured on the repository rather than a public issue.
